rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Open for this one-day draft. Extend the date if you reuse the app later.
      allow read, write: if request.time < timestamp.date(2026, 12, 31);
    }
  }
}
